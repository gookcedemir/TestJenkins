pipeline {
    agent any
    environment {
        NAME = 'Gökce'
    }
    stages {
        stage('Hello') {
            environment {
                SUFFIX = '!'
            }
            steps {
                echo "Hello ${NAME} ${SUFFIX}"
                sh 'printenv'
            }
        }
        stage('Goodbye') {
            steps {
                echo "Goodbye ${NAME}"
                sh 'printenv'
            }
        }
    }
}
